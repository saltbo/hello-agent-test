# Hello Agent Test

A simple Express server that responds with "Hello from Agent Kanban" on `GET /`.

## Setup

```bash
npm install
```

## Run

```bash
npm start
```

The server starts on port 3000 by default. Set the `PORT` environment variable to change it.
