# Hello Agent Test

A simple Express server that responds with "Hello from Agent Kanban".

## Setup

```bash
npm install
```

## Run

```bash
npm start
```

The server starts on port 3000 (override with `PORT` env var). Visit `http://localhost:3000/`.
